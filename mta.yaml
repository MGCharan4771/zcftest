_schema-version: "3.2"
ID: zcftest
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zcftest-destination-content
  type: com.sap.application.content
  requires:
  - name: zcftest-destination-service
    parameters:
      content-target: true
  - name: zcftest-repo-host
    parameters:
      service-key:
        name: zcftest-repo-host-key
  - name: zcftest-uaa
    parameters:
      service-key:
        name: zcftest-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zcftest_html_repo_host
          ServiceInstanceName: zcftest-html5-srv
          ServiceKeyName: zcftest-repo-host-key
          sap.cloud.service: zcftest
        - Authentication: OAuth2UserTokenExchange
          Name: zcftest_uaa
          ServiceInstanceName: zcftest-xsuaa-srv
          ServiceKeyName: zcftest-uaa-key
          sap.cloud.service: zcftest
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: zcftest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zcftest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zcftest.zip
      name: zcftest
      target-path: resources/
- name: zcftest
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zcftest-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zcftest-destination-service
    service-plan: lite
- name: zcftest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zcftest-xsuaa-srv
    service-plan: application
- name: zcftest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zcftest-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
